
    else if(reg[regexLen-1] == '+')
    {
        if (reg[regexLen-2] == reg2[regexLen-1])
        {
            cout<<"Matched"<<endl;
        }
        else
        {
            cout<<"No match"<<endl;
        }
    }
    else
    {
        if (reg[regexLen-1] == reg2[regexLen-1])
        {
            cout<<"Matched"<<endl;
        }
        else
        {
            cout<<"No match"<<endl;
        }
    }


